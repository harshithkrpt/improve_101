1. Async , Defer Keywords
2. Preload Keywords
3.difference between micro & macro task queue

Micro : MutationObserver, Promise.then
Macro : setTimeout


Event Loop
    - picks micro tasks queue -> executes -> if new micro task queues add then -> all micro complete -> goes to macro task queue -> ..


call, apply , bind usecase 
    - code for throttle & debounce

