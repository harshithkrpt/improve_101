upstream backend {
    server backend1:80;
    server backend2:80;
} # <-- THIS LINE HAD A MISTAKE

server {
    listen 80;

    location / {
        proxy_pass http://backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}

