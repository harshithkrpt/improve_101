# Mini Gmail Clone

## Overview
A Gmail-like microservices application built with Docker, Kubernetes, and Nginx.

## Getting Started
1. Copy `.env.example` to `.env` and fill in variables.
2. Run `docker-compose up --build` for local development.
3. Use `kubectl apply -f k8s/` to deploy to your Minikube cluster.